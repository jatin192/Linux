git branch && git status  && git add . && git status && git commit -m “commit” && git push origin main
_____________________________________________________________________________________________________________




$ lsblk									  		          	  				(List Block )
																		( or View all storage devices)
_______________ Unicode __________________________ vs _____________________ ASCII    _____________________________   
Unicode = all characters from all languages + symbols + emojis     			ASCII = plain English keyboard characters only 
							      (Universal Characters)							    (letters, numbers, basic symbols)
ह 你 ச আ  محبت A a Z z ✓ ✔ ✘ ✨ ★ → ← ↑ │ ─ ┼ 😊 😎 ❤️ 			A a z Z 0 1 9 ! @ # $ % ^ & *- _ + = / ? .
Whereas ASCII is limited to 127 basic English characters 					Unicode has over 140,000 characters

										ASCII ⊂  Unicode
				ASCII is used because it is 100% safe, compatible, small, fast, and works everywhere.
Examples
$ lsblk													$ lsblk --ascii
Uses Unicode characters for pretty tree:								Uses ASCII (simple characters):
├─sda1													|-sda1
└─sda2													`-sda2
_____________________________________________________________________________________________________________
$ cd									  		          	  						(change directory)
$ cd /						  		          		  	          					(Go to the root directory)
$ cd -									  		                  			      (Go to the previously chosen directory)

$ mkdir folder_name	
$ mkdir path/to/folder_name{1..10}								(Create multiple files with an increasing number)	

$ ls
$ ls -1a

$ pwd						  					 						 (Print working directory)
$ touch 1.txt			
$ sudo -i                                            	 		  				 							 (root user😎️)
_____________________________________________________________________________________________________________
$ rm path1/file1 path2/file2 								  						     (Remove specific files)
$ rmdir												  				    (Remove specific directories)
rm -rf */
rmdir: Only deletes empty directories.
    • rm -rf: Deletes directories and their contents, including non-empty ones.

$ rm -r folder    
	or     
$ rm --recursive folder
Flag	Meaning	Example
-r	recursive (delete directories)	$ rm -r folder
-f	force (no confirmation, ignore errors)	$ rm -rf folder ❌ (Dangerous)
-i	interactive (ask before deleting each file)	$ rm -ri folder

$ rm -ri folder2    								 	(✅ Safe Deletion)
It will ask:
rm: descend into directory 'folder2'? y
rm: remove regular file 'folder2/file1'? y
$ sudo rm -rf /  									(⚠️ Danger Zone &&  💀 NEVER run this) 
													It will recursively and forcefully delete everything from 												 	the root directory — basically wiping your OS.
___________________________________________________________________________________________
$ stat File_Fodler_name
$ uptime
$ nautilus  path/path/path
$ whoami
$ exit
$ grep -r "word_find"  folder/				*******************************
$ npm install –legacy-peer-deps
$ command1 | command2 | command3  			vs			$ command1
													$ command2
													$ command3 


Run command 2 if command 1 succeeds:
command1 && command2
    • Run command 2 if command 1 fails:
command1 || command2
    • Run both commands sequentially, no matter the result of command 1:
command1; command2



The pipe (|) is used to pass the output of one command directly as input to another, enabling the combination of simple commands for more complex tasks.
    • You can use it for filtering, transforming, sorting, and processing data in real-time.

$ tldr command (eg. tldr tldr or tlder man)
$ man command
$ cat
$ wget
$ cp
$ mv
$ find
$ chmod
$ lsblk   		(Lists all block devices)
$ nano
$ nautilus
$ touch path/to/file{1..10}					Create multiple files with an increasing number
$ wc -l

$ n
$ n



/tmp is a temporary directory in Linux.
    • It stores files that are deleted automatically after reboot or sometimes after a few days.


/tmp means: the folder named tmp inside root (/)
    • tmp/ means: a folder named tmp inside your current directory


/opt = optional application software
It is part of official Linux Filesystem Hierarchy Standard (FHS).
✔ Meaning:
opt = optional
This folder is used for applications that are not part of the OS — custom software, third-party tools, your own scripts, etc.
✔ Why use /opt?
Because it is:
    • Persistent (does NOT delete on reboot)
    • Clean (keeps your custom apps separate from system files)
    • Standard across Ubuntu/Kali/other Linux distros
    • Good for manual installations (vs /usr/bin which is for package-managed software)
✔ Example:
    • Google Chrome installs in /opt/google/
    • Android Studio installs in /opt/android-studio/
    • You can keep your custom tools in /opt/mytools/



$ nvm ls             
Find out which versions of Node.js you may have installed and which one of those you're currently using:
$ nvm use 20.17.0

# used to broadcast important notifications or announcements to all users currently logged into the system.
$ wall "we will be going down for maintenance for one hour sharply at 03:30 pm"

#combines the functionality of both the ‘uptime‘ and ‘who‘ commands,
# w

___________________________Rearly Used_______________________________________

$ nslookup google.com                                                          # Find the IP Address of a Website

_________________________ Ubuntu software related ________________________________
$ sudo swapoff -a                                                                                            # swap memory off


🧩 What is tldr?

tl;dr stands for “too long; didn’t read.”
It’s a community-driven collection of simplified man pages (manuals) for common terminal commands.


____________________________________________
$ sudo apt update  			
$ sudo apt install -y python3-pip git
$ sudo pip3 install pyudev git+https://github.com/crypto-universe/py_sg –break-system-packages
----------------------------------------------------------------------------------------------------------------------------------------------------
$ lsblk   											            (List connected drives to find your device (e.g., /dev/sdx))
$ cd /tmp												       (Go to temporary folder  → will vanish when rebooted)
$ wget https://raw.githubusercontent.com/0-duke/wdpassport-utils/master/wdpassport-utils.py -O wdpassport-utils.py 																	                                 (Download WD Passport unlock script)
$ chmod +x wdpassport-utils.py												        	         (Make script executable)
$ sudo python3 /tmp/wdpassport-utils.py -u -d /dev/sdx*	(Unlock your WD Passport drive (replace sdx* with actual device))

Flags used:
    • -u → unlock the drive
    • -d /dev/sdx* → specify the device path of your WD Passport drive
____________________________________________
